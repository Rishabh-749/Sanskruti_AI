{
  "name": "sanskruti-ai-monorepo",
  "version": "1.0.0",
  "description": "Root orchestrator for Sanskruti AI Render Deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix Frontend",
    "build-frontend": "npm run build --prefix Frontend",
    "build-backend": "npm run build --prefix backend",
    "move-frontend": "node copy-build.js",
    "build": "npm run install-all && npm run build-frontend && npm run move-frontend && npm run build-backend",
    "start": "npm start --prefix backend"
  }
}
